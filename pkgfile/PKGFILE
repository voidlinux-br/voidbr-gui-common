# Maintainer: Vilmar Catafesta <vcatafesta@gmail.com>

pkgname=voidbr-gui-common
pkgdesc="VoidBR GUI Meta Package"
pkgver=$(date +%Y%m%d)
pkgrel=$(date +%H%M)
provides=()  # "eu SOU o $pkgname pkg1, pra quem depender deles"
replaces=()  # "remove o pkg1 pkg2 antigo automaticamente quando eu instalar"
conflicts=() # "nunca me deixe coexistir com o pkg1 pkg2 original"
arch=('any')
license=('MIT')
#url="ssh://git@github.com/voidlinuxbr/${pkgname}"
url="https://github.com/voidlinuxbr/${pkgname}"
source=("git+${url}.git")
md5sums=('SKIP')
makedepends=()
depends=(
	Adapta             # Tema GTK (arquivos)
	adwaita-icon-theme # Tema de ícones GNOME/GTK (arquivos)
	adwaita-plus       # Extensão do tema Adwaita (arquivos)
	arc-theme
	blueberry
	bluedevil
	blueman
	bluez # Pilha de protocolos Bluetooth
	bluez-alsa
	bluez-deprecated
	cantarell-fonts # Fonte oficial da interface GNOME
	chili-iso2usb   # utilitario para gravar iso em pendrive/device
	dbus-elogind
	dejavu-fonts-ttf
	elogind
	file-roller        # Gerenciador de compactados (GUI)
	firefox            # Navegador Firefox
	firefox-i18n-pt-BR # Firefox em português (Brasil)
	font-awesome       # Fontes de ícones
	font-fira-ttf
	font-firacode
	font-hack-ttf
	font-iosevka # Fonte monoespaçada
	gdk-pixbuf   # Carregamento de imagens (GTK)
	gnome-bluetooth1
	gparted # Editor de partições (GUI)
	gvfs
	gvfs-afc
	gvfs-cdda
	gvfs-gphoto2
	gvfs-mtp
	gvfs-smb
	kitty                 # Modern, hackable, featureful, OpenGL based terminal emulator
	libavif               # Suporte a imagens AVIF
	libheif               # Suporte a imagens HEIF
	libheif-pixbuf-loader # Integração libheif/gdk-pixbuf
	lightdm
	lightdm-gtk-greeter
	lightdm-gtk-greeter-settings
	lm_sensors
	lxdm
	lxdm-theme-vdojo
	mate-calc              # Calculadora (GUI)
	mate-polkit            # Agente de autenticação (GUI)
	mesa-demos             # Ferramentas de teste Mesa (inclui apps com janela)
	mesa-dri               # Drivers gráficos DRI (biblioteca, X e Wayland)
	mesa-vaapi             # Aceleração de vídeo VA-API (biblioteca, X e Wayland)
	mpv                    # Player de vídeo (GUI)
	nerd-fonts-symbols-ttf # Símbolos Nerd Font
	network-manager-applet # Ícone de bandeja de rede (GUI)
	noto-fonts-cjk
	noto-fonts-emoji          # Fontes de emoji da Google
	noto-fonts-ttf            # Fontes Noto (arquivos)
	octoxbps                  # Gerenciador de pacotes (GUI)
	papirus-icon-theme        # SVG icon theme for Linux, based on Paper Icon Set
	pasystray                 # Ícone de áudio (GUI)
	pavucontrol               # Volume control applet for Pipewire
	polkit-elogind
	qt5ct                     # Qt5 Configuration Tool (GUI)
	qt6ct                     # Qt6 Configuration Tool (GUI)
	rxvt-unicode
	sddm                    # Display manager (Fluxbox, Hyprland, Sway, Mango)
	terminus-font           # Fonte de console
	ttf-jetbrains-mono-nerd # Fonte JetBrains Mono Nerd
	ttf-ubuntu-font-family  # Fonte Ubuntu
	udisks2
	voidbr-beauty-icons # Tema de ícones VoidBR (arquivos)
	voidbr-iso-writer   # Gravar ISO em pendrive (GUI)
	voidbr-lightdm-themes
	voidbr-sddm-themes     # Temas do sddm VoidBR
	voidbr-tokyo-theme     # Tema Tokyo Night (arquivos)
	voidbr-wallpapers      # Wallpapers VoidBR (arquivos)
	voidbr-webapps         # Atalhos de webapps VoidBR (arquivos .desktop)
	xdg-desktop-portal     # Camada de comunicação apps/desktop
	xdg-desktop-portal-gtk # Implementação GTK do portal XDG
	xdg-user-dirs          # Gerenciamento de pastas do usuário
	xdg-user-dirs-gtk      # GTK+ tool xdg-user-dirs
	xdg-utils              # Integração com área de trabalho (scripts, X e Wayland)
	xfce4-terminal         # Terminal (GUI)
)
backup=()

package() {
	# Verify default folder
	if [ -d "${srcdir}/${pkgname}/${pkgname}" ]; then
		InternalDir="${srcdir}/${pkgname}/${pkgname}"
	elif [ -d "${srcdir}/${pkgname}" ]; then
		InternalDir="${srcdir}/${pkgname}"
	else
		#local
		InternalDir="${WORKDIR}"
	fi

	cd "${InternalDir}"
	# Copiar a estrutura de /etc, /usr e /opt para /
	local dirs=("boot" "etc" "opt" "usr" "var")
	for dir in "${dirs[@]}"; do
		if [ -d "${InternalDir}/${dir}" ]; then
			cp -a "${InternalDir}/${dir}" "${pkgdir}/"
		fi
	done

	# Install license file if present
	if [ -f "LICENSE" ]; then
		install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	fi

	# Install documentation if present
	if [ -f "README.md" ]; then
		install -Dm644 README.md "${pkgdir}/usr/share/doc/${pkgname}/README.md"
	fi
}

pre_install() {
	log "Iniciando instalação"
}

post_install() {
	log "Finalizando instalação"
}

pre_upgrade() {
	log "Iniciando upgrade"
}

post_upgrade() {
	log "Finalizando upgrade"
}
